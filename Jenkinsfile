pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                sh 'ls'
            }
        }
        
        stage('Build and Test') {
            steps {
                // Run your build and test commands here
                sh 'ls'
            }
        }
    }
}
